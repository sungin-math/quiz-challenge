-- 04_seed.sql — 선생님 계정 등록 + 샘플 문제
--
-- ┌───────────────────────────────────────────────────────────────────────┐
-- │ 실행 전에 반드시 아래 <USER_UID> 와 <EMAIL> 을 바꿔주세요.            │
-- │                                                                       │
-- │ 1. Supabase 대시보드 → Authentication → Users → "Add user"           │
-- │    → "Create new user" 로 선생님 계정을 만듭니다 (이메일 + 비밀번호). │
-- │    Auto Confirm User 를 켜야 바로 로그인할 수 있습니다.               │
-- │ 2. 만들어진 사용자 행의 UID 를 복사해 <USER_UID> 자리에 붙여넣습니다. │
-- │ 3. 같은 계정의 이메일을 <EMAIL> 자리에 붙여넣습니다.                  │
-- └───────────────────────────────────────────────────────────────────────┘

insert into public.teachers(id, email) values ('<USER_UID>', '<EMAIL>');

-- ── 샘플 문제 3개 ────────────────────────────────────────────────────────
-- answers 는 복수 정답 배열입니다. 대소문자와 앞뒤/연속 공백은 채점 시 무시되지만,
-- '10' 과 '십' 처럼 표기가 다르면 다른 답으로 처리되므로 변형을 모두 적어야 합니다.

insert into public.problems (title, body, answers, is_published, order_index) values
(
  '대한민국의 수도는?',
  '대한민국의 수도 이름을 적어주세요.',
  array['서울', '서울특별시', 'seoul'],
  true,
  1
),
(
  '7 x 8 은?',
  '계산 결과를 숫자로 적어주세요.',
  array['56'],
  true,
  2
),
(
  '(비공개 예시) 태양계에서 가장 큰 행성은?',
  '이 문제는 is_published = false 라서 학생 화면에 보이지 않습니다. 관리자 화면에서 공개로 바꿔보세요.',
  array['목성', 'jupiter'],
  false,
  3
);
